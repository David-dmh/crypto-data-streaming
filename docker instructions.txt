docker instructions:

- build image:
    - command = docker build -t property-app .
- run image as container (?):
    - command = docker run --name property-app -it -p 5000:5000 -v $(pwd):/app  property-app
    (?)
    - command = docker run --name property-app -it -p 5000:5000 property-app
